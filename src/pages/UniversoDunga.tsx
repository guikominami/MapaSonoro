import Section from '../components/Section';
import BackButton from '../components/BackButton';

const UniversoDunga = () => {
  return (
    <Section>
      <BackButton linkToGoBack='/exposicao' />
    </Section>
  );
};

export default UniversoDunga;
