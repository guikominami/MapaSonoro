import { imgDataLivros } from '../../assets/data/expoData';
import MainExposition from '../../components/Expo/MainExpo';

const ObrasLiterarias = () => {
  return <MainExposition imageData={imgDataLivros} />;
};

export default ObrasLiterarias;
