import { imgDataUniverso } from '../../assets/data/expoData';
import MainExposition from '../../components/Expo/MainExpo';

const UniversoDunga = () => {
  return <MainExposition imageData={imgDataUniverso} />;
};

export default UniversoDunga;
